from random import randint
apple = Actor("apple")

def draw():
    screen.clear()
    apple.draw()
def place_apple():
    apple.x = randint
    apple.y = randit

def on_mouse_down(pos):
    if apple.collidepoint
       print("Good shot")
       place_apple()
else:
     print("You missed!")
     quit()
